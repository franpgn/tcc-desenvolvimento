# Gabarito de Pull Request

## Objetivo

<!-- O que esta entrega resolve? Qual item do backlog (B-XX) ela materializa? -->

## Alterações realizadas

<!-- Lista objetiva dos arquivos, diretórios e componentes alterados. -->

## Como testar

<!-- Comandos exatos para reproduzir a entrega no ambiente local. -->

```bash
# exemplo
cd cluster && podman-compose up -d
cd ../workload && mvn -q test
```

## Evidências

<!-- Caminhos para artefatos produzidos: logs, summary.json, capturas do TLC, etc. -->

## Relação com o TCC

<!-- Qual(is) linha(s) Tx da Tabela 1 do Cap. 3 §3.3.5 esta entrega realiza?
     Qual(is) seção(ões) da monografia precisa(m) ser atualizada(s)? -->

## Pontos para validação do autor

<!-- O que o autor precisa decidir antes do merge? -->

---

> Após a aprovação do autor, esta entrega pode ser preparada para *merge* em `main`.
> Nenhum *merge* ocorre sem essa aprovação.
